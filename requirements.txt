# JARVIS Voice Assistant - Requirements
# Python dependencies for Raspberry Pi OS 32-bit with Python 3.13

# Audio Recording
sounddevice==0.4.6       # Cross-platform audio I/O with NumPy arrays
numpy==1.24.4            # Compatible with Raspberry Pi 32-bit
scipy==1.10.1            # For WAV file writing

# HTTP Requests (for Deepgram REST API)
requests==2.31.0         # HTTP library for REST API calls

# Groq LLM SDK
groq==0.9.0              # Groq API client for LLM inference

# Future additions (commented out for now):
# python-dotenv==1.0.0   # For .env file support in production
# pyaudio==0.2.13        # Alternative audio library if needed
